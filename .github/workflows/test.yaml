name: Testing for checkout
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    name: Testing for checkout
    steps:
      - uses: actions/checkout@v3
      - name: Custom actions
        run: echo aman

